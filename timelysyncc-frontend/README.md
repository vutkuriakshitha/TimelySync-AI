# TimelySync frontend
#
# See the root README for architecture and full setup:
#   https://github.com/vutkuriakshitha/TimelySync-AI
#
# Local:
#   cp .env.example .env
#   npm install
#   npm start
#
# REACT_APP_API_URL is baked in at build time (important for Render).

### `npm start`
Runs the app in development at http://localhost:3000

### `npm run build`
Production build → `build/`
